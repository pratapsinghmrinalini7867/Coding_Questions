class Solution {
public:
    bool isPrefixString(string s, vector<string>& words) {
        string str = "";
        for(auto x: words){
            str += x;
            if(str == s)
                return(true);
        }
        return(false);
    }
};
